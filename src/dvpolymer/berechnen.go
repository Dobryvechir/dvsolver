package main

func intercessor(manager *dvthermo.ManageInfo) error {
    extra,_ = manager.Extra.(*RechnenInfo)
    if extra.CsvTime < extra.CsvAllTime {
    }
    if extra.SvgTime < extra.SvgAllTime {
    }
    if       
    return nil  
}

func berechnen(manager *dvthermo.ManageInfo) error {
     return manager.Calculate()
}
